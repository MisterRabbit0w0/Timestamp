# timer.cpp

### Args

`interval`: how many seconds counter waiting for

### Outputs

When counter was called, print the timestamp.

When counter was called for 101 times, print the average interval, 50th percentile, 75th percentile, 90th percentile, 95th percentile and 99th percentile.

All the outputs will be saved in folder /results/, you can check them at any time.